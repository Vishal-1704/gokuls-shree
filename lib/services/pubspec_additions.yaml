# Add these to your pubspec.yaml dependencies:

dependencies:
  pdf: ^3.10.8          # Core PDF generation
  printing: ^5.12.0     # Preview + print + share (WhatsApp)
  crypto: ^3.0.3        # Real SHA-256 for document hash
  intl: ^0.19.0         # Date formatting
  supabase_flutter: ^2.3.0

# Add these assets to pubspec.yaml:
flutter:
  assets:
    - assets/images/marksheet.jpg   # your background template
    - assets/images/logo.png        # school logo
    # Optional: embed OCR A Std font for exact match
    # - assets/fonts/OCRAStd.ttf

# Install with: flutter pub get
