<?xml version="1.0" encoding="utf-8"?>
<!-- Launch background splash screen — GSMT.jpeg is a square emblem with
     its own dark wood-textured background baked in, so the screen
     background is dark to match rather than white (which boxed the
     square in an ugly frame). android:gravity="center" (not "fill")
     preserves the square aspect ratio instead of stretching it. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />

    <item
        android:width="260dp"
        android:height="260dp"
        android:gravity="center">
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo" />
    </item>
</layer-list>
